# FNOL Agent
## Customer chat to report an insurance loss

* extract agent for identifying and storing key facts
* chat agent to manage the conversation
* mongo store
* tooling

### To run
```
./gradlew bootRun
cd claim-chat-ui && npm run dev
```